# 원클릭 업무포털 작업 인수인계

작성일: 2026-07-20

## 현재 구현

- Chrome/Edge 최상위 창 검색
- 선택한 브라우저 연결 및 최소화
- 처음에는 화면 우측 하단에 표시되며 드래그로 이동 가능한 2행 작업표시줄 UI
- 흰색 배경과 약 72px 높이의 얇은 조작창
- 1행: 원클릭업무포털, 상태메시지, 나이스, 복무, 출장, 설정
- 2행: 브라우저 선택, 새로고침, 로그인, 연결, 에듀파인, 기안, 품의, 닫기
- DWM 미리보기 제거
- 전용 Edge 프로필 실행과 로컬 DevTools 연결 확인
- 상단 `로그인` 버튼으로 전용 Edge를 실행하고 연결 후 `열기` 버튼으로 브라우저를 표시
- 설정창에서 Windows 시작 시 자동 실행, 업무포털 자동 새로고침, 프로그램 투명도 설정 저장
- 설정창에 현재 버전과 저작권 문구 표시
- 브라우저 연결 시 나이스와 K-에듀파인 자동 실행 및 핵심 메뉴 준비 완료 대기
- 준비 완료 후 연결된 Edge 자동 최소화
- 나이스 및 K-에듀파인 탭 자동 탐색
- 상태 기반 업무 화면 이동
  - 복무: 개인근무상황관리의 근무상황신청 창
  - 출장: 개인출장관리의 출장신청 창
  - 기안: 공용 표준서식 편집기(WXSClient)
  - 품의: 학교회계 품의등록 화면
- 가로 한 줄 업무 버튼: 나이스, 복무, 출장, 에듀파인, 기안, 품의(품의등록)
- 업무 이동 중 조작창을 숨겨 브라우저 작업 화면을 가리지 않음
- 실행 중 진행 상태 표시와 중복 실행 방지
- 다른 나이스 신청창이 열려 있을 때 기존 창을 닫고 선택한 복무·출장 화면으로 전환
- 자동화 대상 탭의 실제 Edge 창 ID를 사용한 정확한 창 최대화
- `%LOCALAPPDATA%\OneClickPortal\logs` 단계별 진단 로그
- 최종 승인·결재 버튼 자동 실행 금지

## 주요 파일

- `MainForm.cs`: 가로형 UI, 브라우저 창 연결, 워크플로우 실행 및 원본 창 복원
- `SettingsForm.cs`: 시작 실행, 자동 새로고침, 투명도, 버전·저작권 설정창
- `DevToolsAutomation.cs`: 로컬 Edge DevTools 검색, 탭 연결, DOM 명령과 실제 마우스·키 입력 전달
- `PortalWorkflowController.cs`: 복무·출장·기안·품의 상태 기반 이동 절차
- `Program.cs`: Windows Forms 진입점
- `BrowserThumbnailPrototype.csproj`: .NET 8 Windows Forms 설정, 실행 파일명 `OneClickPortal`, 현재 버전 `0.1.3`
- `README.md`: 사용자 실행 및 사용 방법

## 검증 결과

2026-07-20 로그인된 테스트 세션에서 다음 화면까지 실제 이동을 확인했습니다.

- 근무상황신청
- 출장신청
- 품의등록
- 표준서식(결재4인,협조4인) WXSClient 편집기

테스트 중 저장, 승인요청, 결재요청, 결재올림은 실행하지 않았습니다.

## 실행 및 빌드

```powershell
dotnet run
dotnet build .\BrowserThumbnailPrototype.csproj --configuration Debug
```

## 다음 개발 시 주의사항

- 나이스와 K-에듀파인은 동적 UI 프레임워크를 사용하므로 화면의 텍스트와 실제 표시 상태를 함께 검증해야 합니다.
- K-에듀파인 상단 메뉴와 대형 메뉴는 자바스크립트 `click()` 대신 DevTools 입력 이벤트를 사용합니다.
- 나이스 메뉴는 DevTools 입력 이벤트, 신청 버튼은 나이스 자체 DOM 클릭 이벤트를 사용합니다.
- DOM 텍스트 확인 시 숨겨진 탭과 닫힌 대화상자를 현재 화면으로 판정하지 않도록 표시 상태를 함께 검사합니다.
- 복무·출장 간 전환 시 현재 열려 있는 나이스 신청 입력창을 닫고 선택한 업무 화면으로 이동합니다.
- 나이스는 연결 중 10분마다 자동 갱신하며, 신청 입력창이 열려 있거나 업무 이동 중이면 갱신하지 않습니다.
- 업무 이동 중에는 Edge를 투명 처리해 보이지 않게 유지하고 입력 화면이 준비된 마지막 단계에서만 복원·최대화합니다.
- 로그인 정보, 인증서 암호, 개인정보, 세션 토큰을 저장하거나 로그로 남기지 않습니다.
- 최종 상신 계열 버튼은 자동화 범위에 넣지 않습니다.
